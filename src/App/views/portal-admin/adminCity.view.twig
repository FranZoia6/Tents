{% extends "base.view.twig" %}
{% block main %}
        <h1>{{ titulo }}</h1>
        <section>
                <section>
                        <a class="bNew" href="/newcity">Nueva Ciudad</a>
                </section> 
                <table class="tents-table">
                        <thead>
                                <th>Nombre</th>
                                <th>Acciones</th>
                        </thead>
                        <tbody>
                            {% for city in cities %}
                                    <tr class="ABM row-{{ loop.index is odd ? "odd" : "even" }}">
                                            <td>{{ city.fields.name }}</td>
                                            <td class="center">
                                                <a title="Editar" href="/portal-admin/cities/edit?id={{ city.fields.id }}&name={{ city.fields.name }}"><i class="fa-solid fa-pen-to-square"></i></a>
                                            </td>
                                    </tr>
                            {% endfor %}                           
                        </tbody>
               </table> 
        </section>
{% endblock %}

{% block footer %}{% endblock %}